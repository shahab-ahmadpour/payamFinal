# راهنمای Config Distributor (تب Admin)

این قابلیت برای ساخت خودکار فایل‌های `IRN-PC####_config.txt` روی شیر `\\irn-st10\payamconf` است تا دیگر لازم نباشد زمان‌ها را دستی حساب و کپی کنید.

## چه چیزی حل می‌شود؟

- اختلاف میلی‌ثانیه بین ۲۰–۴۰ سیستم به‌صورت یکنواخت محاسبه می‌شود
- زمان تکراری جلوگیری می‌شود (در صورت تنگ بودن پنجره، ۱ms جابه‌جا می‌شود)
- کم/زیاد شدن تعداد PC فقط با Scan یا ویرایش لیست + Generate دوباره حل می‌شود
- فرمت فایل کانفیگ همان قبلی می‌ماند؛ کلاینت‌ها همان **Read Config** را می‌زنند

## پیش‌نیاز

1. روی یک PC ادمین، AutoClick را باز کنید
2. در **Settings → Network Share Auth** یوزر/پسورد شیر را ذخیره و **Connect Now** بزنید
3. Config Folder باید `\\irn-st10\payamconf` باشد

## روند استفاده (روز مسابقه)

### ۱) تب Admin را باز کنید

### ۲) لیست سیستم‌ها را پر کنید (یکی از روش‌ها)

- **Scan share**: همه `*_config.txt`های موجود روی شیر را می‌خواند و نام PCها را لیست می‌کند (سریع‌ترین شروع)
- **Load machines.txt**: اگر قبلاً لیست ذخیره شده باشد از `\\irn-st10\payamconf\machines.txt`
- دستی: نام PC را تایپ کنید (مثلاً `IRN-PC0426`) → **Add**
- حذف: انتخاب چندتایی + **Remove**

پیشنهاد: بعد از نهایی شدن لیست، **Save machines.txt** بزنید تا برای دفعات بعد آماده باشد.

### ۳) پنجره زمانی را تنظیم کنید

مثال رایج شما:

| فیلد | مقدار نمونه |
|---|---|
| DATE | 2026/09/16 |
| BASE TIME | 09:29:59 |
| START MS | 700 |
| END MS | 200 |
| PROCESS | Payam |
| CLICK COUNT | 3 |

اگر **END MS < START MS** باشد، برنامه می‌فهمد که پنجره به **ثانیه بعد** می‌رود  
(یعنی از `09:29:59.700` تا `09:30:00.200`).

### ۴) Preview

دکمه **Preview** جدول زیر را نشان می‌دهد:

`# | Machine | TargetTime | File | Status`

خلاصه بالای جدول چیزی شبیه این است:

`32 PC(s) · 09:29:59.700 → 09:30:00.200 · window 500 ms · step ~16 ms`

اگر هشداری بود در تب Logs هم نوشته می‌شود.

### ۵) Generate configs

- تیک **Also save machines.txt** معمولاً روشن بماند
- تیک **Delete configs not in list** فقط وقتی روشن کنید که مطمئنید PCهای خارج از لیست دیگر لازم نیستند
- **Generate configs** → تأیید → فایل‌ها روی شیر نوشته می‌شوند

نمونه خروجی هر فایل:

```text
TargetTime=2026/09/16 09:29:59.700
TargetProcess=Payam
clickCount=3
```

### ۶) روی هر کلاینت

1. AutoClick را باز کنید
2. Payam sync را چک کنید
3. **Read Config** بزنید
4. **START**

## نکات مهم

- ترتیب اسلات‌ها بر اساس **نام PC مرتب‌شده** است (ثابت و قابل پیش‌بینی)
- اگر تعداد PC بیشتر از میلی‌ثانیه‌های پنجره باشد، بعضی زمان‌ها +1ms جابه‌جا می‌شوند و در Logs هشدار می‌آید → پنجره را بزرگ‌تر کنید
- Generate فایل‌های هم‌نام را **overwrite** می‌کند
- Cleanup فقط `*_config.txt` را پاک می‌کند؛ پوشه‌های `logs` / `ntp` دست نمی‌خورند

## فایل‌های مرتبط روی شیر

| فایل | نقش |
|---|---|
| `machines.txt` | لیست رسمی PCهای امروز |
| `IRN-PC0002_config.txt` | کانفیگ هر سیستم (خوانده‌شده توسط کلاینت) |
| `ntp\ntp_config.txt` | سرور NTP (جدا از Distributor) |
